<header>
    <nav>
        <ul>
            <!-- Home Page Link -->
            <li><a href="/polls">View Polls</a></li>

            <!-- Profile Page Link (Only visible when user is logged in) -->
            <% if (user) { %>
                <li><a href="/profile">Profile</a></li>
            <% } %>

            <!-- Logout Link (Only visible when user is logged in) -->
            <% if (user) { %>
                <li><a href="/logout">Logout</a></li>
            <% } else { %>
                <!-- Login Link (For users not logged in) -->
                <li><a href="/login">Login</a></li>
                <li><a href="/signup">Sign Up</a></li>
            <% } %>
        </ul>
    </nav>
</header>
